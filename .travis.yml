language: php

php:
  - 5.6
  - 7.0.21
  - 7.1
  - 7.2

matrix:
  include:
    - php: 5.6
      env: setup=lowest
    - php: 5.6
      env: setup=stable

before_script:
  - composer install --prefer-source --no-interaction

script:
  - vendor/bin/phpunit